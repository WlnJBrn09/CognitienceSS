{
  "name": "cognitience-ss",
  "version": "2.1.0",
  "description": "Cognitience SS — local-first spreadsheet with liquid glass UI and Rust backend",
  "author": "Cognitience",
  "license": "MIT",
  "private": true,
  "scripts": {
    "backend": "cargo run --release",
    "backend:build": "cargo build --release",
    "start": "cargo run",
    "native": "cargo build --release && cargo build --release --manifest-path native-host/Cargo.toml && powershell -NoProfile -Command \"Start-Process -FilePath (Resolve-Path 'native-host/target/release/cognition-ss-native.exe') -WindowStyle Normal\"",
    "native:build": "cargo build --release && cargo build --release --manifest-path native-host/Cargo.toml",
    "native:headless": "cargo build --release && cargo build --release --manifest-path native-host/Cargo.toml && set COGNITION_NATIVE_HEADLESS_SECS=20&& native-host\\target\\release\\cognition-ss-native.exe --headless",
    "icon": "node scripts/make-icon.js",
    "dist": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-native-win.ps1",
    "dist:win": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-native-win.ps1"
  },
  "devDependencies": {
    "png-to-ico": "^2.1.8"
  }
}
